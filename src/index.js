import React from "react";
import ReactDOM from "react-dom";
import App from "./App";

ReactDOM.render(<App />, document.getElementById("root"));

//ReactDOM.render(): Bu fonksiyon, React bileşenlerini gerçek DOM'a render etmek için kullanılır. İki argüman alır: birincisi render edilecek React elemanı veya bileşen, ikincisi ise bu bileşenin nereye render edileceği DOM elementidir.
// render edilecek eleman App
// bilesenin render edilecek yeri root
